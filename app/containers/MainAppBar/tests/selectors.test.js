// import { fromJS } from 'immutable';
// import { makeSelectMainAppBarDomain } from '../selectors';

// const selector = makeSelectMainAppBarDomain();

describe('makeSelectMainAppBarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
