// import { fromJS } from 'immutable';
// import { makeSelectTeamsDomain } from '../selectors';

// const selector = makeSelectTeamsDomain();

describe('makeSelectTeamsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
