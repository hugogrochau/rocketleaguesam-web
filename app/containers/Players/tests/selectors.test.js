// import { fromJS } from 'immutable';
// import { makeSelectPlayersDomain } from '../selectors';

// const selector = makeSelectPlayersDomain();

describe('makeSelectPlayersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
